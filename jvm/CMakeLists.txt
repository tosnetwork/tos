# JVM workchain — TOS-side adapter for the wc=3 JVM execution domain.
#
# Phase 2 scaffold: registers JvmNativeEngine stub (run_compute returns NOT_READY).
# Phase 4 (JvmCellCodec + Avata interpreter integration) makes this executable.

add_subdirectory(core)
