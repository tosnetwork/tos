# Slice 3 External Author Trial

This document records the Stage 8 repo-side surrogate. On 2026-04-30,
the maintainer accepted this surrogate as the Slice 3 release gate for
`actor-layer`. A later non-maintainer trial is still useful validation,
but is no longer required before calling Slice 3 complete.

## Repo-Side Surrogate

The surrogate examples are generated by the real `tol new` command:

| Example | Pattern | Source | Test |
| --- | --- | --- | --- |
| `examples/slice3/jetton-author-trial` | Jetton | `src/main.tol` | `tests/jetton-positive.tol` |
| `examples/slice3/nft-author-trial` | NFT | `src/main.tol` | `tests/nft-positive.tol` |

`scripts/check-slice-3-release-package.py` verifies both examples by
running `tol --check-only`, running their generated tol-tester smoke
tests, and validating manifest/replay/deploy/observability JSON.

## Trial Log

### Run 1 — Repo-side surrogate (2026-04-30)

| Field | Value |
| --- | --- |
| Tester | Claude (surrogate for human — did not implement compiler or stdlib) |
| Pattern | Jetton |
| Start | 2026-04-30 14:48 UTC |
| End | 2026-04-30 14:57 UTC (~9 min) |
| Passed within 1 hour? | Yes |

**Steps completed:**

1. `tol new --pattern jetton --name TrialJetton --output /tmp/trial-jetton` — 5 s, no issues.
2. Inspected `src/main.tol`, `manifest.json`, `artifacts/opcodes.json`, `artifacts/error-codes.json`, `deploy/deploy.json`, `replay/jetton-replay.json`. All present and consistent.
3. Modified contract: added `MAX_SUPPLY` cap and a mint guard `require(totalSupply + amount <= MAX_SUPPLY, ...)`.
4. First `tol --check-only` failure: `JETTON_MINTER_FUNC_THROW_SUPPLY_EXCEEDED` undefined — expected, author must define custom error constants.
5. Second failure: `ErrorClass.BusinessLogic` not a valid member — author looked up `enum ErrorClass` in `tol-stdlib/common.tol`, switched to `ErrorClass.Permanent`.
6. `tol --check-only` passed (exit 0).
7. `tol-tester tests` — 1 test, 1 case, OK.

**Artifact navigation — all located without hints:**

| Item | Location |
| --- | --- |
| Opcode map | `artifacts/opcodes.json` |
| Error-code map | `artifacts/error-codes.json` |
| Deploy skeleton | `deploy/deploy.json` |
| Replay stub | `replay/jetton-replay.json` |
| Wire-compatibility escape hatch | `@unknown_throw(65535)` in `src/main.tol`; `TEP-74 bodies raw` note in `slice-3-jetton-walkthrough.md` |

**Sticking points:**

- `ErrorClass` member names were not in the generated scaffold or walkthrough; author had to grep stdlib.
  Fixed by adding the `ErrorClass` reference table to `doc/slice-3-writing-tol-contracts.md`.
- `error-codes.json` is empty for a fresh scaffold (no custom codes yet), which is correct but may confuse authors expecting pre-populated entries.

**Doc/tool improvements suggested:**

- Done: `doc/slice-3-writing-tol-contracts.md` now lists valid `ErrorClass` members.
- Done: `doc/slice-3-jetton-walkthrough.md` now shows a custom supply-cap error-code example.

---

## Under-One-Hour Path

| Step | Target time |
| --- | --- |
| Generate or open the Jetton/NFT scaffold | 5 minutes |
| Inspect `src/main.tol`, `manifest.json`, and opcode/error maps | 10 minutes |
| Fill project-specific owner/admin/content fields | 15 minutes |
| Run `tol --check-only src/main.tol` and `tol-tester tests` | 10 minutes |
| Inspect replay/deploy stubs and audit checklist | 15 minutes |
| Buffer for local fixes | 5 minutes |

Total target: 60 minutes.

The automated surrogate proves the command path and checked artifacts
exist in-repo. Per the 2026-04-30 maintainer decision, it replaces the
release-gating human sign-off for Slice 3.
