# E15 DNS governance receipt follow-up (2026-09-25)

Status: **local single-validator route passed; independent raw review and owner signoff pending**. The first two exact-tree runs below are failures retained for diagnosis, not passes.

The original corrected-command console is `test/integration/.e15-dns-1477e4e66-corrected-command-20260925-console.typescript`, SHA-256 `5ab15df01b1026b267cfa46d0ab76219e4746396058d82b0a2308f6a11fa1d94`. The retained network directory is `test/integration/.e15-dns-1477e4e66-20260925-network/`; its `rpc-transcript.jsonl` SHA-256 is `088a8254e762f2234dde404ca9fa03b7f272f2c5564161052eacbdae07d5926a`, `registration-evidence.json` is `a2c83504393ae4bd32aa731c12ea88f9b74b0609ff2e54f6acad32d6d303e520`, and `provenance.json` is `44e5eb2b6676843f7a8c1aac9984c1ac4fc5e79aaf64c18f9ab87966e126620d`. Genesis-side registration and prelaunch refusal passed. In governance, the faucet baseline was LT `9000002`; wallet LT `21000001` emitted one message to Config, hash `f5gfnpKVeONDjbh/a0lOWDYxBudWLMgP1FUGcKxw0wI=`. Config LT `21000003` consumed that exact message with compute/action success and emitted a return payment, hash `xe619CW7/r+dcfk+2nIpkZqj4esbAJ4UxTg5Lgnp3zk=`, which the faucet received at LT `21000005` with zero outgoing messages. The old `len(wallet_rows)>1` check called this “multiple new faucet transactions” and exited 1. This is a test-instrument classification error, not an Elector refusal or a second proposal send.

The summary `getTransactions` response has no `in_msg.msg_data.body`. The follow-up uses `getTransactionsStd` for the exact Config LT/hash and decodes its raw transaction BOC with the existing TL-B `Transaction` parser. It checks raw transaction hash, raw inbound message hash against the summary's exact wallet→Config message, and decoded inbound body hash against the locally prepared proposal or node-produced PQ vote body. Extra faucet rows are allowed only if their inbound hash/source matches a Config return from that exact Config transaction and they have no outgoing messages. A second faucet send to Config or an unrelated wallet transaction still fails closed.

The next committed-tree run at `09c247f99` used `env PYTHONPATH=test/tostester/src uv run python -u scripts/dns-e2e.py` under `script -q -e -f`. Its console `test/integration/.e15-dns-09c247f99-20260925-console.typescript` (SHA-256 `1f102de859bc7262607202185dd6d9b1f1bcb2987b1b3700ffd682d1cd367b20`) exited 1. The proposal now has an exact raw-BOC governance receipt (`governance-evidence.jsonl` SHA-256 `20166aa22d985ac1b91430f544901dafbc815a7f834f292b3c2e3f5dc015b42b`) and registered with Config. The node-generated PQ vote was sent, but the subsequent faucet `getTransactions` call hit one 10-second `TimeoutError` at the HTTP transport and aborted the script before the vote receipt or ConfigParam 4 effect could be assessed. The raw RPC transcript is SHA-256 `b60a0e6f4b0ec7797802d984e471d4c03e689262c6156db098a7f0b03f846128`; provenance is `73dbc9393bd70c3e67e383cb80433518a4386a2da5fadd178638e6ec9e131974`. All are retained in `test/integration/.e15-dns-09c247f99-20260925-network/`. This was a diagnostic failure, not evidence that the Config rejected the vote.

The follow-up records `TimeoutError`/`URLError` with the raw request in the transcript and retries only these transport failures while the *same* governance-receipt monotonic deadline remains open. It does not resend the vote, treat HTTP 500/JSON errors as success, or weaken the ConfigParam 4 effect assertion. Offline checks: `python3 -m unittest test.pq-native.test_e15_dns_registration_finality` exited 0 with 15 tests, including a Config return credit, an unrelated-row/second-send refusal, one transient timeout recovery and persistent timeout failure. `python3 scripts/check-dns-pq-vote-source.py /home/tomi/tos-n6` exited 0.

## `934ec43c1` exact-tree successful run

Command: `script -q -e -f -c 'env PYTHONPATH=test/tostester/src uv run python -u scripts/dns-e2e.py' test/integration/.e15-dns-934ec43c1-20260925-console.typescript`. Exit **0**, `ALL PASS`; `provenance.json.source_commit=934ec43c1a707ca2b59d2e32dbf5791e10af409d`. The workdir is preserved at `test/integration/.e15-dns-934ec43c1-20260925-network/`, and no node network process remains.

Both `governance-evidence.jsonl` rows are present. The proposal uses wallet LT `22000001` → Config LT `22000003`, exact message hash `80IN3sX1C5kX0fa8Oo8aMgb16jJIkLyxvJP/br2gkzY=`, body hash `063d555d5aad8979e2895049db3a871f5055215cae59370bf3db46538462cf65`, and finalized MC heads 11/12. The node-produced PQ vote uses wallet LT `29000001` → Config LT `29000003`, exact message hash `bCaT5nyVLx1V6B107b5W4ZaLgKM9KtWNkkL4Qw8VIEc=`, body hash `0e6348a756957aa39d0a068ef39a56f4dfb17cbfaf1155f8cfceb95accfe81cf`, and heads 14/15. In both rows, Config `aborted=false`, `compute.success=true`, `action.success=true`; the transcript retains HTTP 200 `getTransactionsStd` requests for those exact Config LT/hash pairs. The script subsequently asserts `ConfigParam 4 appears after the validator vote` and `resolution works on the running chain after activation`, both PASS.

| artifact | SHA-256 |
| --- | --- |
| console | `29261d4919ffd1ed86bd6301d77df8de8c596d2994429d4f4d63bd5aa067bb7d` |
| RPC transcript | `f511a09227249dac937d83dbf1836ebb65bcab0087211ae8d7808e4c588a2829` |
| governance evidence | `c9cea728e44f96c29399e50fa33794919a81853b07b93b4342f3844d746a5d56` |
| registration evidence | `244d279a2a617088023bb02a1cb2f0eb8f6cf44210e838312a5787d6b55d2f09` |
| provenance | `bc22ed8350f7c1c77dca44a3698413b337de46228e7660ec7bf2167f450e4857` |

The exact executable and generated-contract bytes have also been copied to `test/integration/.e15-dns-934ec43c1-binaries/` and rehashed against `provenance.json`: validator-engine `186fef777bba20168fb48efb43fd78d2945cb16a565a10630e165dc1bbabaa3a`, dht-server `fd76bc835664b97fac56ced447b5f9310d8f6539ec24e3fc1422ec769f64d94d`, fift `ec4f335608595622b828ca96abd1b4fff8d4f2882d7be6ab46ccb5c05c30d51d`, func `3ad2f14b31183c1021c5f6e76ce02b00a5ba075e0086aaadc0ceabba0bcc5cd7`, lite-client `264410d28452ecb7c477a5cb35db03fd014b3a773b3fcf09168c3c6b75bfeade`. The snapshot also contains the collection/root StateInit BOCs and generated DNS Fift code, each matching the provenance hashes; this keeps shared build-path changes from invalidating binary attribution during independent review.

This is a bounded local DNS governance route, not a multi-validator or release-scale DNS claim. E15 should be marked complete only after independent review of these raw receipts and the exact source/binary manifest. The separate E03 shard-ancestry and reorg-ledger risks are unaffected.
