# F01 dcb fourth stake: exact closed-window receipt

F01 remains **OPEN**. The fixed `dcb607406` run exited 1 before any of its three required Config34 transitions. Its first three candidates were accepted, and the duplicate-key probe reached Elector before close and returned reason 4. The restarted fourth validator's order instead reached Elector too late.

The retained report is `test/integration/.f01-stage-a-dcb607406-20260925/20260925T160547Z/report.json` (SHA-256 `a5cbbf1bb1b9b06c5cd8cdc62a4021830b77523c980930e50889e6ba4e57263e`); console is `test/integration/.f01-stage-a-dcb607406-20260925-console.typescript` (SHA-256 `d7381798a872b922c3e1349dc4ad5dd235753e3ee166a4c8b3b4d63179a2885f`). Original node databases and all run artifacts are retained.

Read-only extraction used a disposable copy of node1 and the frozen run validator-engine. The copy was opened on loopback JSON-RPC 27500, queried, and stopped; the original node database was not reopened. Raw HTTP `getTransactions` responses, five decoded transaction BOCs, extraction program and exact route summary are under `test/integration/.f01-dcb607406-forensic/`. The extractor asserted each BOC transaction cell hash against its RPC transaction ID, the five adjacent message hashes, and query ID 4 in the Elector inbound and reply. Its summary SHA-256 is `4e96f917e345d151e537e695d6c777743db894ebaa376f06535a4bba0c2770d2` and extractor SHA-256 is `5f601ffe8243da8d87718e466f0202b4fccf84235b34a87b2227087345cff521`. The five BOC SHA-256 values are in that summary; the Elector transaction BOC is `58ed1a44c99bfaa0fa08f61626956e3864373d973441340e64afaa58b68d974e`.

The exact chain is wallet LT 204000001 → pool LT 204000003 → controller LT 204000005 → Elector LT 204000007 → pool reply LT 204000009. All five transactions have chain `utime=1790352904` in MC H78. Elector inbound is opcode `0x50517374`, query ID 4; its outgoing message and the pool's incoming message share the same hash, opcode `0xee6f454c`, query ID 4, reason 0. The prior raw Elector getter recorded `elect_close=1790352890`, so the Elector input was 14 chain seconds after close. This proves the closed-window predicate was true, but does not separately establish whether the earlier `finished` branch also held. It is a test scheduling failure, not evidence that the key or witness was rejected. Mac's independent archive/BOC audit is in `memo/pq-native/N6-MAC-F01-DCB-FIRST-RUN-FAILURE-FORENSIC-20260925.md`.

The follow-up source change is intentionally scoped: `--f01-extended-election-window` is valid only for Stage-A launch-gate, changes only its opt-in Genesis Config15 `elect_start_before` from 180 to 240 seconds, and leaves default Stage A and production timing unchanged. Every positive PQ pool order now rechecks the same `elect_at`, `elect_close` and chain time after authorization, immediately before wallet send. It also retains its exact Elector inbound BOC/hash/LT/utime and rejects an input at or after close, even if a pool reply was observed. This is a fail-closed test assertion, not a contract or consensus rule change. The next run must read back the generated and live Config15, retain all four pre-send windows and inbound receipts, and still complete three Config34 transitions with four independent native full BlockIdExt observations around each transition.

Local directed command: `PYTHONPATH=test/tostester/src .venv/bin/python -m pytest -q test/pq-native/test_f01_stake_window.py test/pq-native/test_f01_stage_a_evidence.py test/pq-native/test_f01_finalized_ids.py` → 17 passed, 14 subtests; retained log SHA-256 `8165fae93214a0d3a67479cd7c9b6279d22ea155f85322f09bb7e003abb673b0`. The new test requires opt-in profile isolation, same-ID/positive-margin pre-send behavior, and source-guard failure when either pre-send or Elector inbound-time condition is bypassed. Live outcome and F01 acceptance are still unproven on this changed source tree.

The first follow-up tree `2475100a2` was run once and exited 1 at the initial Config15 read-back, before any stake. Its report is `test/integration/.f01-stage-a-2475100a2-20260925/20260925T162643Z/report.json` (SHA-256 `151d999ef6102182f4bf045739008bf78ace062fc578eadaecef3d3f8fe48f05`) and console is `test/integration/.f01-stage-a-2475100a2-20260925-console.typescript` (SHA-256 `6b5fe7e870ddf9ed903d1c52d3c4cf30770ed4ba4ae99b47f74408134aa37ef9`). Raw `artifacts/initial-config.txt` has `300/180/60/180`: the opt-in Python profile had not yet reached the Genesis builder, which hard-coded Stage A's 180 seconds. This is an instrumentation/configuration propagation failure, not a stake or consensus failure; the incomplete F01 capture is consequent early stop. The next source revision adds a Stage-A-only `NetworkConfig.validator_election_stage_a_start_before` override, sends the opted-in 240 value from `configure_network_profile`, and decodes generated Genesis Config15 in a targeted test. Its live read-back remains a separate mandatory gate for the next run.

The fixed Genesis-propagation source passes the full `test_zerostate_supply.py` plus F01 window/capture suites: 46 tests and 14 subtests, raw log `test/integration/.f01-dcb607406-forensic/f01-genesis-window-tests.log` SHA-256 `231aaef5c8eb67eaa31fe32fbde6f77cd03283d32392e0ad0f783a2deeeb73c5`. The generated-BOC test decodes Param15 as `300/240/60/180` and still reads a 600-second initial Config34, while separate invalid/non-Stage-A override controls reject. This is offline Genesis evidence; the live `getconfig 15` value is not inferred from it.
