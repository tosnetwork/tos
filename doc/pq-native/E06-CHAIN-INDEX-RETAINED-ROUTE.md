# E06 chain index retained route — 2026-09-25

Status: local exact-tree PASS; independent review and fixed-head CI pending. The E03 shard-ancestry progress and reorg nominator-ledger risks remain OPEN.

PG's preflight commit `c6f97fa91a26eca483ef23159f3fae64bc2ec1e7` was cherry-picked as `b4fbccaed`; it retains raw HTTP success/error bodies, a fresh-index database precondition and a source/binary manifest. Commit `f5084a609` keeps HTTP waits off the event loop that drains the node/service subprocess logs. Its direct synchronous-call mutation fails `test_http_wait_does_not_block_node_log_drain`; raw red/green logs are `test/integration/.e06-http-loop-blocking-mutant-20260925.typescript` (SHA-256 `be7402baead39a81452543aa20dc4abc495e0552d17783d186f0e70250c9b53b`) and `.e06-http-loop-clean-20260925.typescript` (`168c3ec478366fa8d9c03650d81fa8d64d8f82ac89b953d799fa4e0e8a3a5c37`).

The first real run from `f5084a609a72b1a8404f3c54445db236e3e460c7` exited 1 with 2 failures. The `/tasks` poll remained empty for 60 seconds and the final HTTP `/explorer/status` returned `masterchain_indexed=30`, `masterchain_head=55`; the deployment masterchain anchor was 55, so the indexer had not yet reached the deployment anchor. The preserved database continued to checkpoint 34 after that HTTP snapshot and contained the Task Escrow transaction at shard block 53 and the Capability Registry transaction at shard block 63. This does not prove a permanent missing-task index defect. The full console is `test/integration/.e06-agent-chain-index-f5084a609-20260925-console.typescript` (SHA-256 `64ff5907a4bd42d54081c4cbd8e1a50e599b2c09814550c86cb30b9ffd037552`); node data/DB and HTTP transcript are retained under `.e06-agent-chain-index-f5084a609-20260925-network/`. The old transcript SHA-256 is `84551f5ca488f0c065a510405712373c46e30b2cce2ac38eb7cdf7cb2585fdb8`.

Commit `d6d5b37f5f975ade0e1cb9ba3731053dfe215ceb` adds a bounded precondition that the HTTP index cursor covers the deployment masterchain anchor before checking discovery. It does not merely sleep for a fixed duration. A `>=` to `<=` mutation makes stale cursor 30 appear to cover anchor 31 and fails the targeted test. Raw red/green logs: `.e06-cursor-direction-mutant-20260925.typescript` (SHA-256 `35019991b71c192dee58082bf3e4d66bf1ff2451dbcdfd8c3b3a62ae0fe49111`) and `.e06-cursor-clean-20260925.typescript` (`9739e0e253e06e513768ab4c35dea611e2fc90e8265dfdf137bedbb840d016aa`).

The exact `d6d5b37f5` rerun used `script -q -e -f -c 'TOS_BUILD_DIR=build PYTHONPATH=test/tostester/src uv run python -u scripts/agent-chain-index-e2e.py' test/integration/.e06-agent-chain-index-d6d5b37f5-20260925-console.typescript`. Exit code 0, 26 PASS, zero FAIL, `RESULT: ALL PASS`; console SHA-256 `9c33d842610e600480b0e0afe7b4b9dd697e6358578b105f63d958a65a47b2d7`. The fresh deployment anchor was 30; the HTTP transcript recorded 47 `/explorer/status` polls and a final indexed cursor of 30 before route assertions. Raw transcript SHA-256 `a137dd4e3b66a0fd53bcef0ae7323c03bfc020f713ef6161eaa8825b9cc6de6c`. Full node data, fresh SQLite index, configs and manifest are retained under `test/integration/.e06-agent-chain-index-d6d5b37f5-20260925-network/`; manifest SHA-256 `3ba577af16d91e7c67fa3e7293fb60ef2269b2f8748f4d9ce8ac12fd77cc91fb`, `source_tracked_dirty=false`. Binaries: validator-engine SHA-256 `2b9c840dd17c00190774416c75061b9f6720a63f4f523ab9d1a88aa38abb38a8`, dht-server `a55e3f16efc39a72e3c45f84bc4672b271f9be81d3e4612dbd019f077bff2937`, tosctl `bb60afd03c43519d43f0c3aed0b053110034bcac181d0b4ee6d8c284876316d1`.

The result proves one local PQ chain's config-B service discovered config-A's Task Escrow and Capability Registry after an explicit progress barrier, plus the advertised direct lookups and invalid-query HTTP 400s. It does not prove a large backlog can always make progress beyond the 4096-ancestor limit, nor correct reorg rollback of the nominator ledger.
