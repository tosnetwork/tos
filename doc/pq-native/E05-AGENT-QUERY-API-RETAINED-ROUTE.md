# E05 Agent/Task query API retained route — 2026-09-25

Status: local exact-tree PASS; independent review and fixed-head CI pending. E06/E07/E08 are separate units.

The source tree for the passing run is `f1437aec5bcceb91313bfd00c90c3af5a7987a69`. The command was `script -q -e -f -c 'TOS_BUILD_DIR=build PYTHONPATH=test/tostester/src uv run python -u scripts/agent-query-api-e2e.py' test/integration/.e05-agent-query-api-f1437aec5-20260925-console.typescript`; exit code 0, 38 PASS, zero FAIL, `RESULT: ALL PASS`. The full console is retained at that path (SHA-256 `836ca90fcc0ad7c7eeb5c28f45ebb54e51f0e479e329d70091dce4c3a8db4284`). The unmodified node data, config, and HTTP transcript are under `test/integration/.e05-agent-query-api-f1437aec5-20260925-network/`; the transcript's SHA-256 is `bb2b2c87ee5acc64265009741c24db000ae07d6649f891bc9ffe6dc6cbc559c9`. Binaries: `build/validator-engine/validator-engine` SHA-256 `2b9c840dd17c00190774416c75061b9f6720a63f4f523ab9d1a88aa38abb38a8`, `build/dht-server/dht-server` `a55e3f16efc39a72e3c45f84bc4672b271f9be81d3e4612dbd019f077bff2937`, `tosctl/src/target/debug/tosctl` `bb60afd03c43519d43f0c3aed0b053110034bcac181d0b4ee6d8c284876316d1`.

The route deployed an Agent Account and three Task Escrows, then checked the actual open, accepted and settled states, list/filter results, malformed-address responses, and two calibrated 404 `not_found` responses. The exact negative bodies are in the transcript; both returned `code=404, kind=not_found`, not a generic non-200. It exercises one local PQ validator and real HTTP, not release-scale or multiple validators.

Two earlier retained failures are distinct. At `b20580f88`, `agent task send accept` reported `Transaction timeout expired`; the retained chain shows its exact wallet external inbound, wallet outgoing and Task Escrow incoming message hashes, successful compute/action, and accepted Task state. The read-only forensic raw capture is `test/integration/.e05-agent-query-api-b20580f88-20260925-forensic/capture-accept-effect/raw.json` (SHA-256 `ac488d8baa1b1dcc441f94090ab7036a26cddf1ceeb121cbfad430502b9de281`); `scripts/e05_task_send_forensic.py` records the RPC exchanges and asserts the exact outgoing/incoming match. This is a false CLI timeout, not an Elector or contract refusal. Commit `96b98be05` changes the shared Task-send wallet helper to build once and confirm the exact BOC without ambiguous resend. Its old short-seqno-poll mutation failed the targeted Rust unit while the restored group passed 10/10; the old-red log is `test/integration/.e05-agent-query-api-b20580f88-20260925-forensic/old-short-poll-mutant.typescript` (SHA-256 `90d318589e529194946246e4fe2792d08bd1872204eadb0eb1a2f17d027aa45a`). Other users of this shared helper still need their own route evidence.

At `96b98be05`, the full route reached HTTP but the two negative probes returned 504 `get_task_data query timed out`, while other HTTP checks passed. Console: `test/integration/.e05-agent-query-api-96b98be05-20260925-console.typescript` (SHA-256 `85f13c10f363981b1c78d25e24d1137bdac7b924f7229c3044f2b6b914f6ea30`); transcript SHA-256 `53f8fbbcc247d9d6fe3f74b74963497b98a9a62d06d9493321f1531b322c5051`. The E05 script used synchronous `urlopen` on the event loop that also drains node and service logs. Commit `f1437aec5` moves these HTTP calls to a worker thread. The focused non-blocking test passed 3/3 clean; changing only the helper back to a direct synchronous call failed `test_http_wait_does_not_block_node_log_drain`, with the HTTP task timing out before log drain. Raw red/green logs: `test/integration/.e05-http-loop-blocking-mutant-20260925.typescript` (SHA-256 `d4cd4494f440dd178fe2ba67a3f14d01e2a232856549c0887beaad2f66a3527a`) and `test/integration/.e05-http-loop-clean-20260925.typescript` (`70b790d66784ddbc84606468a74275a21802146528937a98cfb154aa3f57f2a9`). This proves the scheduling control and that the new exact run passed; it does not independently prove that event-loop blocking caused every earlier 504.

This entry does not close the independent E03 shard-ancestry progress or reorg ledger risks, and does not classify any historical Merkle incident.
