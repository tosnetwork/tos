# A03 development ledger candidate

This is an offline, fail-closed inventory, not A03 signoff. The frozen task table is `memo/main@ae2be260e355277893453d4883407d2b1feb06b6`, `pq-native/N6-AGENT-WORK-UNITS-20260924.md`, SHA-256 `07b72bd03967aa27cef81184394b4c323e86edeef882312a54fc79750bbed596`. This is an integration on `n6/measurement-contract`, not a new real-chain run.

`a03-task-snapshot.json` fixes all 72 task IDs, status symbols, lanes and owners. The checker parses the fixed table and requires every snapshot row to restate its ID, order, lane, owner and status; a matching table SHA alone does not validate a stale snapshot. `a03-development-ledger.json` gives each ID a separate row. The 58 task-table signed rows currently have explicit unresolved machine-evidence gaps. This does not reverse their human signoff; it means A03 cannot assert that their original raw artifacts and review have been individually reconciled. The 10 development rows marked `□` or `▶` also block the gate. The four `◇` rows are testnet-only and never satisfy a development row. The three O-2 deferred measurements remain in `N6-OPEN-MEASUREMENT-GAPS.json`; they are release blockers, not development results.

The signed rows now hold a checked *partial* index: 57 fixed memo report references across 49 rows, 60 hash-verified raw or evidence files across 21 rows, and 20 fixed source-file blob references across 14 rows. Each of the 58 rows lists its remaining field gaps. The index contains only paths explicitly cited by that task-table row or its directly linked report; a referenced source object or artifact does not by itself establish runtime provenance. Nine signed rows (`M01`, `E03`, `T01`–`T05`, `F02`, `I01`) have no directly linked review report in the frozen table. The checker rehashes memo objects at the frozen memo commit and local raw files, rejects Git tree objects as source files, and distinguishes a SHA literal in a cited report from a SHA independently computed for a cited path. It still rejects every unreconciled signed row.

Run `python3 scripts/a03_development_ledger.py --snapshot doc/pq-native/a03-task-snapshot.json --ledger doc/pq-native/a03-development-ledger.json --repo . --evidence-root /path/to/frozen/evidence --memo-repo /path/to/memo --memo-ref origin/main`. It must currently exit 1. Its output reports `counts`: `accepted` (reconciled and reviewed rows, currently 0), `signed_unreconciled` (58), `open` (10) and `deferred_testnet` (4). The checker compares the live memo task-table bytes with the frozen table SHA; a changed table fails until the snapshot is deliberately updated. To reconcile a signed row, record its fixed source commit and file SHA, exact command and exit, distinct raw artifacts and SHAs for the main run and old-red/new-green/mutant-red controls, binary SHA or explicit gap, independent review artifact, CI state/reason, applicability and invalidating changes. Set `accepted=true` only after that row is reviewed. A pending CI is recorded and a later CI failure invalidates the row; it is never silently treated as green. Source files are checked against `git show <fixed-commit>:<path>` and artifacts against raw bytes.

The `ae2be260` snapshot is a deliberate re-freeze after A03 owner changed to `CC + PQ + Mac`. Its exact snapshot SHA is `8b5811a6b4c775bd1487ae9b31c20f89e39c80a278097148ff03ffa902415ec5`. The first-batch inventory exits 0 with no accepted IDs, while the development gate exits 1 solely for 58 unreconciled signed rows and 10 open development rows. Local raw logs are retained under `test/integration/.a03-cc-545-integration-20260926/`: 42/42 unittest exit 0 (SHA `4e90da18416adce640ad05c0492d2d55244e46dd91f1262619a04bbadf042a4a`), inventory exit 0 (SHA `ced142671a58a746ac2366262e97f701944e5533f4ac32105ba0b73fcac1fde3`), and expected gate exit 1 (SHA `c50b95620d85e9fc49e9fb0928ce97f1374f06c87f6c4b458510bc5a9e956d55`). This is an offline integration only; no business unit's original signoff is expanded.

Each of the four runs also needs a distinct SHA-checked JSON receipt. Its exact keys are `schema`, `unit_id`, `role`, `source_commit`, `argv`, `exit`, and `raw_sha256`; `schema` is 1 and `role` is `main`, `old_red`, `new_green`, or `mutant_red`. Every control records its own command argv. The checker matches receipt contents to that unit's fixed source, command, exit, and original raw output digest, so editing ledger metadata without updating the retained receipt fails. A receipt is a binding record, not proof that the claimed process actually ran; process, binary, and reviewer attribution still need their independent original evidence before a row can be accepted.

This candidate does not infer missing N3/N4 historical outputs from later N5/N6 evidence, does not close an open Merkle incident from a mechanism repair, and does not use F/X/Q/Z/I or offline candidates to close neighboring tasks. The task snapshot must be deliberately updated with a new memo commit and trusted SHA when task-table statuses change. The current `ae2be260` table retains X01 as `✅` and Z01 as `▶`, and assigns A03 to `PQ` and the other eight changed owners explicitly; the row-by-row comparison checks that change. X01's signed row is indexed only for the fixed `6e863142b` single-host four-process stop/restart Stage A window, with `accepted=false` and six listed gaps; it carries nothing for X02. Final G-2 integration requires evidence from I04's exact Ubuntu 24 tree and I05 independent review.

This owner/prose snapshot refresh does not promote accepted rows. Ledger source_commit84a remains inherited provenance metadata, not the execution source of this refresh.
