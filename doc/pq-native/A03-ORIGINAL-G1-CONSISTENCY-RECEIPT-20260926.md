# Original G-1 fixed mapping consistency receipt

Executed data tree: `7bcee93c7ab1585d8355f6ba4e7660cdd4e5c649`, tracked clean before and after; task table: memo `ae2be260e355277893453d4883407d2b1feb06b6`. Mapping SHA-256 `c6e98912d16ef7bcbf3dc9921aeada33d43c5ee9643092b245bfb35876a83986`. This documentation successor does not change or relabel that execution.

Reviewed tool `/datax/n6-unit-agents/A03/verify-original-g1-7bcee-v2.py`, SHA-256 `25dbc9820ad83c22507e49c28a902b0ff23068e88d33649fd7373be30005cb52`. Exact argv: `/usr/bin/python3 -I -S -B /datax/n6-unit-agents/A03/verify-original-g1-7bcee-v2.py`. One-use resource unit `n6-heavy-1790403503-169970` naturally exited **0** (resource receipt started `1790403503.6647317`, ended `1790403503.8932018`; systemd reported 187 ms service). Source/blob comparison, exact 72 distinct snapshot IDs, direct original-table owner/status/workline, complete scoped paragraphs and each original line SHA, ten registry applicability IDs, three O-2 deferrals and still-unreviewed release criteria passed.

Original job output directory: `/datax/n6-unit-agents/A03/original-g1-7bcee-v2/`. `receipt.json` binds the five exact input hashes and command; `actual.stdout.raw` is the tool's serialized positive validation result, not a separately captured systemd/outer stdout transcript. The overall natural exit is `/datax/n6-control/runs/n6-heavy-1790403503-169970.json`; `actual.exit.raw` is written only after controls and post-checks. No invented outer shell triplet.

Five negative controls perturb row SHA, fixture-versus-retirement type, FAA green source, coordinated map/snapshot owner, and duplicate snapshot ID. They each trigger the intended same-process `ValueError`. Their generated `.exit.raw=1` files are **derived assertion markers**, not five child-process natural exit codes or historical production mutants. Raw error text is retained beside the receipt.

The earlier v1 unit `n6-heavy-1790403229-161245` actually passed the fixed positive data, but omitted direct table-column and snapshot uniqueness guards; its output remains immutable. V2 closes that finite metadata contract. Neither job authenticates historical artifact files or reconstructs absent receipts/binaries/unique mutation patches. Original human scoped signoffs remain; machine accepted IDs remain empty, software/release gates stay fail closed. This supports independent review of the original G-1 **ledger mapping delivery**, not automatic A03 signature or G-2 acceptance.

The native build is separately bound to `f1f912dafd2dc3120e92829ec1858941bc426ec9`; its mutable cache and future executable freeze must not be represented by this metadata receipt. X02 private compatibility errors and the public Lite route also remain separate units.
