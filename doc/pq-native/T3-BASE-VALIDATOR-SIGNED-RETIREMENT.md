# T11: retire the installed classical signed validator command

`crypto/smartcont/validator-elect-signed.fif` produced the classical `0x4e73744b` signed stake body after checking a 64-byte Ed25519 signature over the classical `0x654c5074` request. Its pre-T11 source SHA-256 was `82694a04104f4731028428ee2bd40c842735a13362568fd0909f9290a0cf7b55`, mode `100755`. That wallet-to-elector body is not the PQ pool/controller path. After T07, `crypto/test/test-smartcont.cpp` loads `crypto/test/fift/fixtures/validator-legacy-elect-signed.fif` (SHA-256 `d1e04d6d4acc1863d8bfb53d4c2bc73d407d4a9f4b9b54986138ebfee3005e3f`) for its recorded historical byte check; a source scan found no remaining executable inclusion of the product signed **file**. T09's historical stake parity is also test-local. The filename in `Validator.fif` was only a comment about its transitive `Key.fif` include and was updated to name the actual test-only fixture users.

The product signed file is removed from the installed `smartcont` tree. The literal caller inventory now lists six executable files, down from seven at T10. The guard refuses restoration of either base script. `Validator.fif` still defines the classical words and is still installed; its test-only callers and T12 remain **OPEN**. This removal is not a PQ stake acceptance test and does not close T3.

Raw output and the reversible old-file patch are retained under `test/integration/.t11-signed-validator-retirement-20260924/` (not Git assets):

| Check | Result | Raw SHA-256 |
| --- | --- | --- |
| Before/after `build/test-smartcont --regression test/regression-tests.ans --filter ValidatorFiftScriptRegression` | both exit 0, 1/1; recorded answer unchanged | `4613349b37a926325097ba3b739b559aad51e38186170ff38b71195beaf7573e` / `e7833e60af9e289773cc06d3317985b10fbd9c297749f9a0655a4bbb0c49c4ad` |
| After `build/test-fift --regression test/regression-tests.ans --filter test_validator_proposal_legacy_parity` | exit 0, 1/1 | `5c6d058e904b42a3257e4aed37d408a20b0a2db36427de8d3f934be30116be9b` |
| Before/after `python3 scripts/check-classical-stake-callers.py .` | both exit 0; seven then six exact files | `600d5e1930590ef339f0b784e8a4ef98ba1e95092e01217be7e1619010bfc652` / `befc268eedb0d8009a555720a9045a36a8981a02211805a56eda868c34602f67` |
| `ctest --test-dir build --output-on-failure -R '^(classical-stake-callers-source\|test-fift\|test-smartcont)$'` | exit 0, 3/3 | `da49caeb52e1310c8b00e7749c6ca78efa080a6e92d05d1a96afa13f39ca2375` |
| `./scripts/check-regression-db.sh . build` | exit 0; every frozen answer still has a test | `9f7996a7f3408e1e200d281f0cea92f2a1b2d5a322d8a4201d02b14ed3565fc6` |

The frozen answer file is SHA-256 `9653b4411d3c7213f6b6845b6cec231a5b97e8fdf2101244cdbab41c69125e20`; pre-existing test binaries: `build/test-smartcont` `d728fbc3dafae21bd3d5d2c94750487281a4706e0444bc47ec490b6f71868570`, `build/test-fift` `2a5b914d75460bee87c2d81c61a2069a02686203df96643442dea3e0e2297984`.

The single old-product reverse control is `old-product-signed-mutant.patch` (SHA-256 `9f2134723a576c5d1671de436c4b4cc5c77fb8ed03e8fa896ac43b37d8510a9f`, `git apply --check` exit 0). It restores the old file's exact bytes **and executable mode**; the temporary source had SHA-256 `82694a...` and `git diff --quiet HEAD -- crypto/smartcont/validator-elect-signed.fif` exited 0. The guard then exits 1, specifically `retired base Ed25519 signed-body script is still packaged` (`old-product-signed-mutant.raw.log` SHA-256 `86b027404c21270f0c4696e438b52fddfbd361ac015f48c4bcc778fcc4eeae06`). The file was removed again before the clean CTest. A positive control remains the unmodified T07 fixture's recorded-answer test, which still produces its frozen answer without the product command.

The T10 fresh local release-shaped ZIP list (`.t10-base-request-retirement-20260924/release-full-list.raw.log`, SHA-256 `5ce69cd9a5c6defc1dc67b623d0b57bbd5dae42706be4927ad322f012d355a27`) contained `smartcont/validator-elect-signed.fif` and `lib/fift/Validator.fif`, with the request file already absent. A new **fresh-prefix** `cmake --install build --prefix test/integration/.t11-signed-validator-retirement-20260924/install` (`install.raw.log` SHA-256 `7add8f2603f5f040800736f3325cb8b1ebb2b0d1d300f0696869dba11f340eac`) installs `Validator.fif` but neither base command. Both release workflows copy installed `smartcont` and `lib` into `smartcont_lib.zip`. A local full-tree ZIP from those exact two installed directories is SHA-256 `742325a4523b7623737e198bc824233ec9be98020efc9aeeed658e531d80f036`; its complete `unzip -Z1` listing, SHA-256 `ec01f1b8407e8587df017d9fee41c6781743786ab0d40f223bb6bc0789da3e31`, lists `lib/fift/Validator.fif` and neither base script. This reproduces the packaging shape; it does **not** attest to already published release assets.

T12 must independently isolate the shared library's historical users and verify its own installed/ZIP boundary. No T12 retirement is claimed here.
