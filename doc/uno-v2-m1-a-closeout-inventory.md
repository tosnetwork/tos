# A's M1 closeout inventory under the closed-gate boundary

2026-09-10; source baseline `33af64ee8`. Owner definition: mechanisms complete,
seams prepared, and the gate demonstrably closed with correct classification.
No live connection or opening permission is claimed. This is A's inventory,
not a replacement inventory for B or a declaration that M1 is accepted.

## Already delivered and archived

- `70b53ec8f`: I13b bounded execution ledger, actor members and private acceptance;
  separate duplicate/bound/allocation outcomes. No live enforcement claim.
- `5d8125ed5`: I13a bounded independent identity/count scanner and private
  acceptance, with explicit representation provenance. No live scan claim.
- `88fee8306` checkpoint, superseded by reviewed `99160542e`: private I13c/d
  coverage reconstruction and staged read observer; typed exception nature,
  source-dependent caller contract, final-source controls. The coverage
  companion records 37 final-source controls; old cohorts are historical.
- `98508fcfe`: single binding owner, explicit terminal release retained,
  transport-only two-root carrier and private controls; explicitly unconnected.
- `b2eeef278`: independent AccountBatch scope pairing and private fallback
  controls. `33af64ee8` records the eventual real-call-site obligation and bans
  dormant call sites invented to discharge it.
- Private harness mounting is already six names in
  `.github/scripts/check-i13-results.py`, including ledger, scan, and coverage.
  Existing manual workflow remains dispatch-only. This is not coverage by the
  ordinary test suite.

These are not items awaiting implementation again. Their final integration
compatibility still needs the integration acceptance below.

## Completed investigation, documentation not yet committed

- `doc/uno-v2-c2-c3-closure-survey.md`: full scope, dependencies and bounded effort
  estimates; distinction between work-budget debit and money; observed ingress
  restrictions and absent punishment in the examined report path; joint exposure
  gap. Update includes the final owner decision to defer opening.
- The implementation plan's current scheduling paragraph is updated to the new
  definition and the shared registry frontier. Acceptance: archive these changes
  as documentation, with no production gate change. Historical runtime evidence
  must remain labelled historical.

## Not yet done: A's closed-gate frontier evidence

Superseded status (2026-09-10): the focused collator observation is now archived
in `doc/measurements/uno-a2-registry-closed/README.md` on merged tree
`63aacc17989a9ff1fad17c2bcb05e0d5d8c14eb8`. Earlier-failure calibration precedes
the gate run; all five layers and raw sidecars are retained. This awaits review,
does not create an integration merge commit, and is not live validator evidence.
The original requirements and limitations below are preserved for traceability.

One shared readiness rule has two real host call sites: collator fetch and
validator fetch. The current-source acceptance record must distinguish:

1. entry to validate_required_workchains;
2. successful resolution to AccountBinding;
3. the particular account readiness refusal, not an earlier local failure;
4. final typed LocalUnavailable/abstention, with no candidate-invalid outcome;
5. downstream visitor counts zero, zero execution/transactions and no candidate
   export, with confirmed observation delivery rather than missing output.

Use actual authenticated fixture inputs and passive observation. Neither bypass
the registry nor infer location from -7201. Calibrate the instruments and the
classifier against an earlier failure so that silence or another local error
cannot satisfy the gate claim. No fresh result is asserted here. Existing
first-seam traces are a historical collator checkpoint, not this acceptance.

B's newly reported merged-tree readiness run may discharge some or all of the
collator part after its exact source, typed result, observation sites, and
artifacts are inspected. Do not demand a duplicate run merely because B produced
it. Its applicability to a real validator invocation is unconfirmed. Acquisition
of any still-missing validator fixture needs coordination; A owns the frontier
evidence, not an invented parallel candidate producer.

## Not yet done: final integration closeout

After an authorized merge, inspect the actual source/evidence set and perform
the agreed complete default build (-j32), ordinary regression with fresh
per-case disposition, private six-name static registration check, and wire/tag
guard. Do not copy A's old nine deferrals: B reports those paths now pass.
Ordinary regression still excludes private harness execution. Re-run controls
whose measured sources changed; do not relabel unchanged historical controls as
fresh runs. Then obtain the repository-required milestone review and address
its findings. Merge timing/ownership remains with the coordinator.

## Scope clarification requiring coordinator ownership, not guessed completion

The shared carrier is delivered. The unused combined acquisition callback in
validator.h does not structurally prevent deriving declarations from candidate
bytes; it has not been frozen as a compliant independent acquisition design.
If "seams prepared" requires this acquisition API now, that is an outstanding
preparation item needing an explicitly scoped owner. If deferred with actual
connection, keep the obligation recorded; do not silently count it as satisfied.

## Opening-side obligations, not missing M1 live evidence

- Actual carrier-overload call-site guard and mutation once that call site exists.
- Coroutine adapter identity across real execution suspension, and real
  execution completion/cancellation release evidence; private owner evidence
  is not a substitute, and dormant calls must not be added to manufacture it.
- Live I13a-e enforcement, validator reconstruction and request-binding/
  mandatory-verification correctness at the concrete engine boundary.
- C2/C3 live enforcement and joint exposure acceptance before permission to move
  D45. No claim that the concrete engine verifies the correct statement follows
  from the work meter or this M1 definition.

These obligations survive deferral; they are not waived or reported complete.
