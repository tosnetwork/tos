# Renamed context schema controls

Thirteen isolated removals are measured against source commit 9f6e93a25.
Every mutant syntax-checks, fails exactly its designated schema vector, and
restores byte-for-byte. Reapplying the recorded edit reproduces the mutant hash.
This is a record-validator unit, not an activation classifier or live host
transaction/export observation. The consumer directory records integration with
the single shared helper; neither artifact opens a deployment or final gate.
