The approved path grants only Uno domain membership. The existing scanner still rejects an unrelated removed execution domain at the same path. Source is pinned in report.json; JSON escapes decode to the exact mutation bytes. Gzip files preserve complete stdout and stderr, including captured empty output. This is scanner-input evidence, not an activation or application guard. The comment mutation has no compiled executable holders.
